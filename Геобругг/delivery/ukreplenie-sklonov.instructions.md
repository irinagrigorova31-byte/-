# Инструкция к сдаче — /ukreplenie-sklonov/

Файл: `delivery/ukreplenie-sklonov.html` (идентичен `preview/`). Самодостаточный HTML:
инлайн-CSS с токенами дизайн-системы, шрифты Manrope + PT Sans с Google Fonts, JSON-LD в `<head>`.

## Что заменить перед публикацией (важно)
1. **Фотографии.** Hero и две карточки «Систем» используют диаграммную SVG-подложку (ромбическая
   сетка на градиенте) как временную заглушку — она помечена комментарием в коде. Дизайн-система
   требует реальное документальное фото монтажа/систем в поле с тёмной подложкой (scrim). Подставьте
   фирменные фото по путям, где сейчас `.hero-media` и `.card-media`, сохранив scrim.
2. **Логотип.** Знак «ГЕОБРУГГ» отрисован инлайн-SVG (тёмный треугольник + красная грань) + текст.
   Замените на официальный растровый/векторный логотип из `assets/brand/`, когда будет.
3. **Ссылки политики/согласия и якоря меню** — проверьте реальные URL на боевом сайте
   (`/privacy/`, `/consent/`, `#primenenie` и т. д.), при необходимости поправьте.

## Открытые вопросы клиенту (числа [to-check] — не выдуманы, ждут пруфа)
- Прочность сетки (кН/м), срок службы (лет), точная гарантия — на странице НЕ проставлены числом.
  Дайте подтверждение — добавим с датировкой.
- Вес «около 5 кг/м²» и «5–50 USD/м²» взяты с текущей страницы сайта и датированы «на 2026 год».
  Если значения обновились — пришлите актуальные.
- Формулировка статуса: используем «российское подразделение группы Geobrugg AG» (по ЕГРЮЛ —
  участник Geobrugg AG, Швейцария). Слово «производитель в РФ» намеренно не использовано.
- ОДМ 218.2.098-2018 (Росавтодор) — ссылка dofollow-заглушена как `rel="nofollow"` на внешний PDF;
  тело документа не сверено (ограничение окружения). Сверьте точное название документа при желании.

## Разметка (Schema.org)
Organization (NAP из ЕГРЮЛ, parentOrganization Geobrugg AG — не в sameAs) · Service + Offer
(прайс-ориентир) · FAQPage · BreadcrumbList. AggregateRating и warranty НЕ добавлены (нет
верифицированных данных). Каждое значение разметки присутствует в видимом тексте.

## Техчек
Гомоглифы — чисто (review/homoglyph-report.md). JSON-LD — валиден.
